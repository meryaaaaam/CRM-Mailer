<form method="post" action="{{ path('modelesms_delete', {'id': modelesm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modelesm.id) }}">
    <button class="btn">Delete</button>
</form>
