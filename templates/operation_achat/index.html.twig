{% extends 'base.html.twig' %}

{% block title %}OperationAchat index{% endblock %}

{% block body %}
    <h1>OperationAchat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numserie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for operation_achat in operation_achats %}
            <tr>
                <td>{{ operation_achat.id }}</td>
                <td>{{ operation_achat.numserie }}</td>
                <td>
                    <a href="{{ path('operation_achat_show', {'id': operation_achat.id}) }}">show</a>
                    <a href="{{ path('operation_achat_edit', {'id': operation_achat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('operation_achat_new') }}">Create new</a>
{% endblock %}
