{% extends 'base.html.twig' %}

{% block title %}OperationVente{% endblock %}

{% block body %}
    <h1>OperationVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation_vente.id }}</td>
            </tr>
            <tr>
                <th>Numserie</th>
                <td>{{ operation_vente.numserie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operation_vente_index') }}">back to list</a>

    <a href="{{ path('operation_vente_edit', {'id': operation_vente.id}) }}">edit</a>

    {{ include('operation_vente/_delete_form.html.twig') }}
{% endblock %}
