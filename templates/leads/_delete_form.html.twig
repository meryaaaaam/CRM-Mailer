<form method="post" action="{{ path('leads_delete', {'id': lead.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lead.id) }}">
    <button class="btn">Delete</button>
</form>
